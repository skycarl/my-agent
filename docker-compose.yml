services:

  app:
    container_name: app
    build: 
      context: .
      dockerfile: .docker/Dockerfile
    command: bash -c "uv run uvicorn app.main:app --host 0.0.0.0 --port 8001 --use-colors"
    tty: true
    ports:
      - 8001:8001
    restart: always
    networks:
      - app-network
    volumes:
      - .:/app/workspace
    environment:
      - STORAGE_PATH=/app/workspace/storage
      - TERM=xterm-256color
      - FORCE_COLOR=1

  telegram-bot:
    container_name: telegram_bot
    build: 
      context: .
      dockerfile: .docker/Dockerfile
    command: bash -c "uv run python -m telegram_bot.main"
    tty: true
    depends_on:
      - app
    restart: always
    networks:
      - app-network
    environment:
      - TERM=xterm-256color
      - FORCE_COLOR=1

  mcp-server:
    container_name: mcp_server
    build: 
      context: .
      dockerfile: .docker/Dockerfile
    command: bash -c "uv run python -m my_mcp.main"
    tty: true
    ports:
      - 8002:8002
    restart: always
    networks:
      - app-network
    volumes:
      - .:/app/workspace
    environment:
      - GARDEN_DB_PATH=/app/workspace/storage/garden_db.json
      - TERM=xterm-256color
      - FORCE_COLOR=1

  email-sink:
    container_name: email_sink
    build: 
      context: .
      dockerfile: .docker/Dockerfile
    command: bash -c "uv run python -m email_sink.main"
    tty: true
    depends_on:
      - app
    restart: always
    networks:
      - app-network
    environment:
      - TERM=xterm-256color
      - FORCE_COLOR=1

networks:
  app-network:
    driver: bridge